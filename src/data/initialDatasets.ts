import { Dataset } from '../types';

/**
 * Live on-chain datasets listed through Moreman.
 * Defaults to empty array when no datasets have been registered on-chain yet.
 */
export const INITIAL_DATASETS: Dataset[] = [];
